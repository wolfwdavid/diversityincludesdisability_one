---
phase: quick-260924-dd3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/creative/+page.svelte
  - src/routes/about/+page.svelte
  - src/lib/data/nav.ts
  - static/creative/eman-runway.avif
  - static/creative/eman-runway.webp
  - static/creative/eman-runway.jpg
  - static/creative/eman-clip-1.mp4
  - static/creative/eman-clip-2.mp4
  - content/media/eman/MANIFEST.md
  - e2e/shell.spec.ts
  - src/lib/components/Footer.svelte
  - src/routes/contact/+page.svelte
  - src/routes/get-involved/+page.svelte
  - src/lib/config.ts
  - e2e/forms.spec.ts
  - e2e/scaffold.spec.ts
autonomous: true
requirements: [PAGE-02, PAGE-07, PAGE-08]

must_haves:
  truths:
    - "Visitor sees a Creative item in the primary nav between About and Programs & Services, and /creative/ renders an h1 'Creative', a Pictures section with the runway photo, and a Videos section BELOW it with both clips"
    - "The runway figcaption credits One World Peace Co. at The Model Experience show, New York Fashion Week; the string 'One World Worldwide' no longer appears anywhere in the built site"
    - "The About page no longer shows the photo or a Watch section, and its founder bio ends with a link to the Creative page"
    - "Every visible contact email on the site is diversityincludesdisability@gmail.com; emanrimawi@gmail.com no longer appears in the built site"
    - "All five media files are served under /creative/ with correct content types; nothing is left under static/about/"
  artifacts:
    - path: "src/routes/creative/+page.svelte"
      provides: "New Creative page: intro, Pictures (figure), Videos (two clips)"
      contains: "One World Peace Co."
    - path: "src/lib/data/nav.ts"
      provides: "Creative nav entry"
      contains: "{ href: '/creative', label: 'Creative' }"
    - path: "static/creative/eman-runway.jpg"
      provides: "Moved media (plus .avif/.webp and eman-clip-1/2.mp4 siblings)"
    - path: "content/media/eman/MANIFEST.md"
      provides: "Corrected show/designer credit and note about the stale file slug"
      contains: "One World Peace Co."
  key_links:
    - from: "src/lib/data/nav.ts"
      to: "src/routes/creative/+page.svelte"
      via: "Header/Footer render {base}{item.href}; adapter-static strict prerender discovers /creative/ through this link"
      pattern: "/creative"
    - from: "src/routes/creative/+page.svelte"
      to: "static/creative/*"
      via: "{base}/creative/eman-*.{avif,webp,jpg,mp4} src/srcset"
      pattern: "\\{base\\}/creative/eman-"
    - from: "src/routes/about/+page.svelte"
      to: "/creative"
      via: "inline link at end of founder bio"
      pattern: "href=\"\\{base\\}/creative\""
---

<objective>
Apply Eman's 2026-09-24 feedback in one quick pass: (1) move the runway photo and the two clips off the About page onto a NEW "Creative" page (pictures first, videos below), (2) correct the show/designer credit everywhere — show = The Model Experience for New York Fashion Week, designer = One World Peace Co. (the previous "One World Worldwide" was a misread of the backdrop), and (3) switch every contact email from emanrimawi@gmail.com to diversityincludesdisability@gmail.com.

Purpose: The live site currently credits the wrong designer, shows the founder's personal Gmail, and buries her creative work in the founder bio. Eman asked for all three to change.
Output: `src/routes/creative/+page.svelte`, nav entry, trimmed About page, media under `static/creative/`, corrected MANIFEST, email swapped in 4 src files + 2 e2e specs, and a verified static build. Two atomic commits on `main`, nothing pushed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/RESUME-2026-09-23-media.md
@src/routes/about/+page.svelte
@src/lib/data/nav.ts
@content/media/eman/MANIFEST.md

<locked_decisions>
- Owner: build a NEW Creative page (not a section on About, not the Programs page).
- Owner: credit = show + designer only. NO photographer line anywhere.
- Eman (verbatim): "The show was put on by The Model Experience for NY Fashion Week and the designer I walked for was One World Peace Co. This picture should also be in a different area, with the videos. Maybe on the creative page below the pictures. The email should also be the diversityincludesdisability@gmail.com"
- Commit on `main`, one commit per code task, do NOT push (owner pushes). Commit messages describe the change only — no mention of Claude/AI assistants.
- Do not rename anything under `content/media/eman/originals/` or `web/` (file slugs keep the old "one-world-worldwide" name; document it instead).
</locked_decisions>

<interfaces>
<!-- Everything the executor needs; no exploration required. -->

From src/lib/data/nav.ts (single source of truth; Header AND Footer render `{base}{item.href}`):
```ts
export const navItems = [
	{ href: '/', label: 'Home' },
	{ href: '/about', label: 'About' },
	{ href: '/programs', label: 'Programs & Services' },
	...
] as const;
```

Page pattern (src/routes/programs/+page.svelte): `<script lang="ts"> import { base } from '$app/paths'; </script>`, `<svelte:head>` with `<title>X — Diversity Includes Disability</title>` + meta description, single `<h1>`, `<p class="lede">`, `<section aria-labelledby="id"><h2 id="id">`, scoped `<style>` using tokens `--space-*`, `--color-border`, `--radius`, `--measure`, `--color-text-muted`.

Current media markup + styles to MOVE (src/routes/about/+page.svelte lines 24-39, 54-71, 82-112): the `<figure class="founder-photo">` with `<picture>` (avif/webp sources, jpg img, width="985" height="943", loading="lazy", decoding="async"), the `<ul class="clips" role="list">` with two `<video controls preload="metadata" playsinline width="176" height="144" aria-label=…>` each holding a `<source type="video/mp4">` + MP4 download fallback link, the two `<!-- MEDIA: … -->` comments, and the `.founder-photo` / `.founder-photo img` / `.founder-photo figcaption` / `.clips` / `.clips video` CSS rules.

Old email occurrences (exact, all must change): src/lib/components/Footer.svelte:16 (href + text), src/routes/contact/+page.svelte:37 and :76 (href + text), src/routes/get-involved/+page.svelte:83 (href + text), src/lib/config.ts:5 (comment), e2e/forms.spec.ts:37 (regex `/emanrimawi@gmail.com/i`), e2e/scaffold.spec.ts:17 (regex). Historical docs under .planning/ are NOT touched.

e2e/shell.spec.ts hard-codes the route list and nav labels:
```ts
const ROUTES = ['/', '/about/', '/programs/', '/get-involved/', '/events/', '/blog/', '/contact/'];
...
test('all 7 pages reachable from header nav (PAGE-08)', ...
	for (const label of [/home/i, /about/i, /programs/i, /get involved/i, /events/i, /news/i, /contact/i]) {
```

Build/preview env (Windows Git Bash — RESUME-2026-09-23-media.md): ALWAYS prefix `MSYS_NO_PATHCONV=1 BASE_PATH=/diversityincludesdisability_one` on build AND preview, else MSYS rewrites the base path to a Windows path and svelte.config.js rejects it. adapter-static runs `strict: true` — the new route is prerendered because the nav links to it; no config change needed. Pre-existing known svelte-check failure: one `@types/node` error — that one is allowed, nothing else.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Creative page, move the media, trim About, fix the credit, extend the nav + shell spec</name>
  <files>src/routes/creative/+page.svelte, src/lib/data/nav.ts, src/routes/about/+page.svelte, static/creative/eman-runway.avif, static/creative/eman-runway.webp, static/creative/eman-runway.jpg, static/creative/eman-clip-1.mp4, static/creative/eman-clip-2.mp4, content/media/eman/MANIFEST.md, e2e/shell.spec.ts</files>
  <action>
Preflight: `git status --short` must be empty and `git branch --show-current` must print `main`. Stop if not.

1. Move media with git so history follows the files (run from repo root):
   `mkdir -p static/creative && git mv static/about/eman-runway.avif static/about/eman-runway.webp static/about/eman-runway.jpg static/about/eman-clip-1.mp4 static/about/eman-clip-2.mp4 static/creative/`
   then `rmdir static/about` (it is now empty; `ls static/about` must fail).

2. Add the nav entry in `src/lib/data/nav.ts` — insert `{ href: '/creative', label: 'Creative' },` directly after the About line and before Programs & Services. Nothing else changes (keep `as const`).

3. Create `src/routes/creative/+page.svelte` (Programs/About style; 2-space indent like About):
   - `<script lang="ts"> import { base } from '$app/paths'; </script>`
   - `<svelte:head>`: `<title>Creative — Diversity Includes Disability</title>` and `<meta name="description" content="Photos and video from Eman Rimawi-Doster's creative work: runway modeling for representation, an adaptive clothing line, poetry, and graphic novels." />`
   - `<h1>Creative</h1>`
   - Intro `<p class="lede">` reusing ONLY existing About/Programs wording, no new claims: "Alongside her advocacy, Eman Rimawi-Doster models for representation, has launched an adaptive clothing line, and works as a poet and graphic-novel creator. Photos and video from her runway work are below."
   - `<section aria-labelledby="pictures">` with `<h2 id="pictures">Pictures</h2>`, the comment `<!-- MEDIA: supplied by Eman 2026-09-22; show/designer credit from Eman 2026-09-24; alt text is a draft pending her sign-off. -->`, then the `<figure class="founder-photo">` moved verbatim from About EXCEPT: every `{base}/about/` becomes `{base}/creative/`; the `alt` keeps its full description but ends `…in front of a One World Peace backdrop.`; the `<figcaption>` becomes exactly `Eman walking for One World Peace Co. at The Model Experience show, New York Fashion Week.` Keep `width="985" height="943" loading="lazy" decoding="async"` and both `<source>` elements (avif then webp) and the jpg `<img>`.
   - `<section aria-labelledby="videos">` BELOW the pictures section with `<h2 id="videos">Videos</h2>`, the comment `<!-- MEDIA: two phone clips supplied by Eman 2026-09-22. Captions/transcripts still needed (WCAG 1.2.2). -->`, then the `<ul class="clips" role="list">` moved verbatim EXCEPT: paths `{base}/about/` → `{base}/creative/` (both `<source src>` and both download `<a href>`), and aria-labels become `Clip 1 from The Model Experience runway show, 21 seconds` and `Clip 2 from The Model Experience runway show, 60 seconds`. Keep `controls preload="metadata" playsinline width="176" height="144"`, the `<source type="video/mp4">`, and the "Your browser can't play this video. Download clip N (MP4, …)" fallbacks.
   - `<style>`: `.lede { font-size: 1.125em; color: var(--color-text-muted); max-inline-size: var(--measure); }` plus the `.founder-photo`, `.founder-photo img`, `.founder-photo figcaption`, `.clips`, `.clips video` rules copied verbatim from About.
   Heading order stays h1 → h2 → h2 (the shell spec checks no skipped levels).

4. Edit `src/routes/about/+page.svelte`: delete the MEDIA comment + entire `<figure class="founder-photo">…</figure>` (lines 24-39); delete the entire `<section aria-labelledby="watch">…</section>` (lines 54-71); append to the END of the last founder-bio paragraph (the one ending "…poet and graphic-novel creator.") the sentence ` See photos and video from her runway work on the <a href="{base}/creative">Creative page</a>.`; remove the now-unused `.founder-photo*` and `.clips*` rules — that empties the `<style>` block, so delete the whole `<style>…</style>`. The `import { base }` stays (still used by the new link and the Our-work links). Do NOT touch the org-approved mission paragraph.

5. Edit `content/media/eman/MANIFEST.md`:
   - Line 5: replace `Wired into \`/about/\` on 2026-09-23 via \`static/about/\`` with `Wired into \`/creative/\` on 2026-09-24 via \`static/creative/\` (first wired into \`/about/\` on 2026-09-23, then moved at Eman's request)`.
   - Table row 1, "What it is" cell: change `"One World Worldwide" backdrop` to `"One World Peace" backdrop (designer: One World Peace Co.; show: The Model Experience, New York Fashion Week)`.
   - Add a new section after the table, before "## Placement candidates":
     ```
     ## Credit (from Eman, 2026-09-24)

     - Show: The Model Experience, New York Fashion Week.
     - Designer walked for: One World Peace Co.
     - The earlier "One World Worldwide" wording was a misread of the backdrop. The file slugs
       (`…runway-one-world-worldwide.*`) keep that old name on purpose — originals are never renamed.
     ```
   Leave the rest (including "Ask Eman for the original photo") as-is. Do NOT rename any file under content/media/eman/.

6. Edit `e2e/shell.spec.ts`: ROUTES becomes `['/', '/about/', '/creative/', '/programs/', '/get-involved/', '/events/', '/blog/', '/contact/']`; the nav-label loop gains `/creative/i` after `/about/i`; rename that test title to `all 8 pages reachable from header nav (PAGE-08)`.

7. Sanity greps before committing: `grep -rn "about/eman" src static` returns nothing; `grep -rn "One World Worldwide" src static e2e` returns nothing; `grep -c "creative/eman" src/routes/creative/+page.svelte` is 7 (2 srcset + 1 src + 2 source + 2 download links).

8. Commit (stage exactly these paths): `git add src/routes/creative/+page.svelte src/routes/about/+page.svelte src/lib/data/nav.ts static/creative static/about content/media/eman/MANIFEST.md e2e/shell.spec.ts` then
   `git commit -m "feat(creative): add Creative page for runway photo and clips, correct show credit" -m "Move the runway portrait and both clips from About to a new Creative page (Pictures, then Videos), add it to the primary nav, and credit the show and designer per Eman: The Model Experience for New York Fashion Week, One World Peace Co. About links to the new page; media now lives under static/creative/."`
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hugginface_profile/Websites/Eman/diversityincludesdisability_one" && test -f src/routes/creative/+page.svelte && grep -q "One World Peace Co. at The Model Experience show, New York Fashion Week" src/routes/creative/+page.svelte && grep -q "in front of a One World Peace backdrop" src/routes/creative/+page.svelte && grep -q 'aria-label="Clip 1 from The Model Experience runway show, 21 seconds"' src/routes/creative/+page.svelte && grep -q 'aria-label="Clip 2 from The Model Experience runway show, 60 seconds"' src/routes/creative/+page.svelte && test $(grep -c "creative/eman" src/routes/creative/+page.svelte) -eq 7 && grep -q "{ href: '/creative', label: 'Creative' }" src/lib/data/nav.ts && ! grep -q "<figure" src/routes/about/+page.svelte && ! grep -q 'aria-labelledby="watch"' src/routes/about/+page.svelte && ! grep -q "<style>" src/routes/about/+page.svelte && grep -q 'href="{base}/creative">Creative page</a>' src/routes/about/+page.svelte && ! test -d static/about && for f in eman-runway.avif eman-runway.webp eman-runway.jpg eman-clip-1.mp4 eman-clip-2.mp4; do test -f "static/creative/$f" || exit 1; done && ! grep -rq "One World Worldwide" src static e2e && grep -q "One World Peace Co." content/media/eman/MANIFEST.md && grep -q "/creative/" content/media/eman/MANIFEST.md && grep -q "'/creative/'" e2e/shell.spec.ts && grep -q "/creative/i" e2e/shell.spec.ts && git log -1 --format=%s | grep -q "feat(creative)" && test -z "$(git status --short)"</automated>
  </verify>
  <done>/creative route file exists with Pictures then Videos, corrected figcaption/alt/aria-labels and all 7 media references under {base}/creative/; nav has Creative between About and Programs; About has no figure, no Watch section, no style block, and links to the Creative page; static/about/ is gone and the 5 files sit in static/creative/ (git-tracked rename); MANIFEST carries the corrected credit + slug note; shell.spec covers 8 routes/labels; one commit on main, tree clean.</done>
</task>

<task type="auto">
  <name>Task 2: Switch the contact email to diversityincludesdisability@gmail.com everywhere in src and e2e</name>
  <files>src/lib/components/Footer.svelte, src/routes/contact/+page.svelte, src/routes/get-involved/+page.svelte, src/lib/config.ts, e2e/forms.spec.ts, e2e/scaffold.spec.ts</files>
  <action>
1. Replace every `emanrimawi@gmail.com` with `diversityincludesdisability@gmail.com` in exactly these six files (both the `mailto:` href and the visible link text where present; the regex literals in the two e2e specs; the comment in config.ts). Edit each occurrence, or restrict sed to the six paths: `sed -i 's/emanrimawi@gmail\.com/diversityincludesdisability@gmail.com/g' src/lib/components/Footer.svelte src/routes/contact/+page.svelte src/routes/get-involved/+page.svelte src/lib/config.ts e2e/forms.spec.ts e2e/scaffold.spec.ts`. In config.ts also change the parenthetical `(CONFIRMED 2026-07-06)` to `(org address per Eman 2026-09-24)` so the comment stays truthful.

2. Confirm nothing else in the shipped tree still carries the old address: `grep -rn "emanrimawi" src static e2e tests scripts` must return nothing. Files under `.planning/` are historical records and stay untouched.

3. Confirm the new-address occurrence count: `grep -o "diversityincludesdisability@gmail.com" src/lib/components/Footer.svelte src/routes/contact/+page.svelte src/routes/get-involved/+page.svelte src/lib/config.ts e2e/forms.spec.ts e2e/scaffold.spec.ts | wc -l` → Footer 2 + contact 4 + get-involved 2 + config 1 + forms.spec 1 + scaffold.spec 1 = 11.

4. Commit: `git add src/lib/components/Footer.svelte src/routes/contact/+page.svelte src/routes/get-involved/+page.svelte src/lib/config.ts e2e/forms.spec.ts e2e/scaffold.spec.ts` then
   `git commit -m "fix(contact): use the organization email address" -m "Replace the founder's personal Gmail with diversityincludesdisability@gmail.com in the footer, contact and volunteer forms, the Web3Forms setup note, and the e2e locators."`
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hugginface_profile/Websites/Eman/diversityincludesdisability_one" && ! grep -rq "emanrimawi" src static e2e tests scripts && test $(grep -o "diversityincludesdisability@gmail.com" src/lib/components/Footer.svelte src/routes/contact/+page.svelte src/routes/get-involved/+page.svelte src/lib/config.ts e2e/forms.spec.ts e2e/scaffold.spec.ts | wc -l) -eq 11 && grep -q 'href="mailto:diversityincludesdisability@gmail.com"' src/lib/components/Footer.svelte && git log -1 --format=%s | grep -q "fix(contact)" && test -z "$(git status --short)"</automated>
  </verify>
  <done>Zero `emanrimawi` hits in src/static/e2e/tests/scripts; 11 occurrences of the org address across the six files; one commit on main, tree clean.</done>
</task>

<task type="auto">
  <name>Task 3: Verify types, static build, served routes/media, and grep the build output</name>
  <files>build/ (generated, untracked)</files>
  <action>
Run every step from the repo root in Git Bash. Record each result for the SUMMARY. Nothing in this task is committed (build/ is untracked output).

1. Types: `npm run check`. PASS condition: the ONLY error is the pre-existing `@types/node` one. Any error mentioning `creative`, `about`, `Footer`, `contact`, `get-involved`, or `nav` is a failure — fix and re-run.

2. Build: `MSYS_NO_PATHCONV=1 BASE_PATH=/diversityincludesdisability_one npm run build`. Must exit 0 (adapter-static strict mode fails the build if /creative/ were not prerendered). Then confirm output shape: `ls build/creative` shows `index.html eman-runway.avif eman-runway.webp eman-runway.jpg eman-clip-1.mp4 eman-clip-2.mp4`; `ls build/about` shows only `index.html`.

3. Preview on a private port, in the background:
   `MSYS_NO_PATHCONV=1 BASE_PATH=/diversityincludesdisability_one npx vite preview --port 4193 --strictPort` (use the Bash tool's run_in_background, then poll `curl -s -o /dev/null -w "%{http_code}" http://localhost:4193/diversityincludesdisability_one/` until it returns 200, max ~20 s).
   Then:
   ```
   B=http://localhost:4193/diversityincludesdisability_one
   for p in /creative/ /about/; do curl -s -o /dev/null -w "$p %{http_code}\n" "$B$p"; done
   for f in eman-runway.avif eman-runway.webp eman-runway.jpg eman-clip-1.mp4 eman-clip-2.mp4; do echo "== $f"; curl -sI "$B/creative/$f" | grep -i -E "^HTTP|^content-type"; done
   for f in eman-runway.jpg eman-clip-1.mp4; do curl -s -o /dev/null -w "/about/$f %{http_code}\n" "$B/about/$f"; done
   ```
   Expected: `/creative/ 200`, `/about/ 200`; five `HTTP/1.1 200` with content-types `image/avif`, `image/webp`, `image/jpeg`, `video/mp4`, `video/mp4`; the two old `/about/…` asset URLs must NOT come back 200 with an image/video content-type (404 or the SPA fallback HTML is fine). If the preview server reports `application/octet-stream` for `.avif` only, note it as a preview-server MIME gap (GitHub Pages serves image/avif) — not a build failure.
   Also assert the rendered HTML: `curl -s "$B/creative/" | grep -c "One World Peace Co. at The Model Experience show"` → 1; `curl -s "$B/creative/" | grep -o '<h2[^>]*>[^<]*</h2>'` prints Pictures before Videos; `curl -s "$B/about/" | grep -c 'href="/diversityincludesdisability_one/creative"'` → 1; `curl -s "$B/" | grep -c '>Creative<'` ≥ 2 (header nav + footer nav).
   Kill the preview: `PID=$(netstat -ano | grep ":4193" | grep LISTENING | awk '{print $5}' | head -1); [ -n "$PID" ] && taskkill //F //PID "$PID"`; confirm `curl -s -o /dev/null -w "%{http_code}" http://localhost:4193/` no longer returns 200.

4. Grep the build output (must-have truths):
   `grep -rl "emanrimawi@gmail.com" build | wc -l` → 0
   `grep -rl "One World Worldwide" build | wc -l` → 0
   `grep -rl "diversityincludesdisability@gmail.com" build | wc -l` → ≥ 1
   `grep -rl "One World Peace" build | wc -l` → ≥ 1

5. Tests: `npm run test:unit` (vitest: validation, posts, theme — must pass) and `npm run test:no-secret` (walks src/ and build/; must pass). Playwright e2e: check `ls "$LOCALAPPDATA/ms-playwright" 2>/dev/null`; if browsers are installed run `MSYS_NO_PATHCONV=1 npx playwright test --config playwright.theme.config.ts e2e/shell.spec.ts e2e/content.spec.ts e2e/scaffold.spec.ts e2e/forms.spec.ts` and require green; if browsers are NOT installed, skip and state "Playwright e2e skipped: browsers not installed" in the SUMMARY — do not install browsers as part of this quick task.

6. Final state: `git status --short` shows nothing (build/ and .svelte-kit/ are gitignored); `git log --oneline -3` shows the two new commits on top of b156f20; `git status -sb` shows `main` ahead of origin — do NOT push.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hugginface_profile/Websites/Eman/diversityincludesdisability_one" && MSYS_NO_PATHCONV=1 BASE_PATH=/diversityincludesdisability_one npm run build >/dev/null 2>&1 && test -f build/creative/index.html && for f in eman-runway.avif eman-runway.webp eman-runway.jpg eman-clip-1.mp4 eman-clip-2.mp4; do test -f "build/creative/$f" || exit 1; done && ! test -f build/about/eman-runway.jpg && test $(grep -rl "emanrimawi@gmail.com" build | wc -l) -eq 0 && test $(grep -rl "One World Worldwide" build | wc -l) -eq 0 && test $(grep -rl "diversityincludesdisability@gmail.com" build | wc -l) -ge 1 && test $(grep -rl "One World Peace" build | wc -l) -ge 1 && grep -q 'href="/diversityincludesdisability_one/creative"' build/about/index.html && npm run test:unit >/dev/null 2>&1 && npm run test:no-secret >/dev/null 2>&1 && test -z "$(git status --short)"</automated>
  </verify>
  <done>svelte-check clean except the known @types/node error; static build succeeds with /creative/ prerendered and all 5 assets under build/creative/ (none under build/about/); preview served /creative/ and /about/ 200 and all five media 200 with correct MIME types, then was killed; build output has zero old-email / "One World Worldwide" hits and at least one new-email / "One World Peace" hit; vitest + no-secret green; Playwright either green or explicitly noted as skipped; working tree clean, two commits on main, not pushed.</done>
</task>

</tasks>

<verification>
- `src/routes/creative/+page.svelte` renders h1 Creative → h2 Pictures (figure) → h2 Videos (two clips), all media under `{base}/creative/`.
- Nav order: Home, About, Creative, Programs & Services, Get Involved, Events, News, Contact.
- Credit text is exactly the locked wording; no photographer line anywhere.
- About: no media, no Watch, bio ends with the Creative link; mission paragraph untouched.
- Old email and "One World Worldwide" absent from src/, static/, e2e/, and build/.
- `static/about/` removed; MANIFEST documents the credit and the intentionally stale slug; originals untouched.
- Build + preview + unit tests green; e2e status stated.
- Two commits on `main`, none pushed, no AI mentions in messages.
</verification>

<success_criteria>
All three tasks' `<automated>` checks pass; the SUMMARY lists the two commit hashes, the svelte-check result, the five content-type lines from the preview, the four build-grep counts, and whether Playwright ran or was skipped. Owner's remaining action is only `git push origin main`.
</success_criteria>

<output>
After completion, create `.planning/quick/260924-dd3-eman-feedback-creative-page-with-runway-/260924-dd3-SUMMARY.md` (quick-task summary: what changed, commit hashes, verification results, anything skipped, and the open items that still need Eman: alt-text sign-off, clip captions/transcripts for WCAG 1.2.2).
</output>
